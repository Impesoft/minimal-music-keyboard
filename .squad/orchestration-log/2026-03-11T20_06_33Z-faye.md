# Orchestration Log — Faye (2026-03-11T20:06:33Z)

**Agent:** Faye (Audio Dev)  
**Tasks:** Catalog Fix + VST3 Hosting Research  
**Status:** ✅ Completed

## Deliverables

### 1. Catalog Fix: 6→8 Instruments

**File changed:** `src/MinimalMusicKeyboard/Services/InstrumentCatalog.cs`

**Change:** Added two instruments to `BuildDefaultCatalog()`:
1. **Fingered Bass** — PC 33 (General MIDI), Category: Bass
2. **Choir Aahs** — PC 52 (General MIDI), Category: Choir

**Rationale:**
- `AppSettings.cs` declares 8 button mapping slots (indices 0-7). `BuildDefaultCatalog()` only provided 6 default instruments, leaving slots 6–7 unmapped on fresh installs.
- New instruments complete the 8-slot mapping and add rhythmic foundation (bass) + vocal timbre (choir)
- Both are common in General MIDI soundfonts (high compatibility)

**Impact:**
- Fresh installs: get all 8 default instruments immediately
- Existing users: no change (their `instruments.json` is already on disk)
- No breaking changes: `LoadOrCreateDefault()` behavior unchanged

### 2. VST3 Hosting Research

**File created:** `docs/vst3-dotnet-options.md`

**Findings:** Evaluated 5 approaches for hosting VST3 plugins in .NET 8 WinUI3 Windows app.

**Key Result:** No production-ready NuGet package exists for VST3 hosting:
- VST.NET: VST2 only; VST3 stubs incomplete
- NPlug: Plugin *creation* SDK, not host
- AudioPlugSharp: Plugin creation with C++/CLI

**Recommended approach: Direct COM P/Invoke (Option A)**
- VST3 plugins are COM DLLs on Windows
- Load via `LoadLibrary` + `GetProcAddress("GetPluginFactory")`
- Define `[ComImport]` interfaces for VST3 COM hierarchy (IPluginFactory3, IComponent, IAudioProcessor, IEditController)
- Estimated complexity: ~600 LOC P/Invoke scaffolding + marshaling
- Main risks: COM reference counting errors, ProcessData struct marshaling, no crash isolation

**Fallback: Out-of-process bridge (Option C)**
- If Option A proves unstable (plugin crashes), pivot to native bridge (C++ or Rust)
- Bridge loads VST3 via Steinberg SDK; communicates via named pipes
- Pro: crash isolation, security boundary
- Con: +5-10ms latency, must ship native binary, ~1200 LOC total

**Action Plan:**
1. Spike documents COM interface hierarchy and threading model
2. Faye implements Option A as `Vst3Host` class in `Services/`
3. Test with Steinberg HALion Sonic SE (free, stable VST3 instrument)
4. Monitor for crashes; pivot to Option C if needed

**Audio Pipeline Integration:**
- Forward NoteOn/NoteOff/PitchBend/CC#64 (sustain pedal) to both MeltySynth and VST3 instrument
- Mix outputs in `IWaveProvider.Read()` callback: `finalSample = (meltySynthSample * gain1) + (vst3Sample * gain2)`
- User controls mix balance via UI setting

## Related Files

- `src/MinimalMusicKeyboard/Services/InstrumentCatalog.cs` — added 2 instruments
- `docs/vst3-dotnet-options.md` — new research document
- `.squad/agents/faye/history.md` — updated Learnings section

## References

- Faye's decision: `.squad/decisions/inbox/faye-catalog-fix-and-vst3-options.md`
- Related: Spike's VST3 backend architecture proposal (bridges Option A findings into abstract backend design)
