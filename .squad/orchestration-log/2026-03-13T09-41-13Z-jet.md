# Jet — VST3 Bridge Deployment Finalization

**Date:** 2026-03-13  
**Status:** Complete  
**Work:** Versioned bridge deployment + manifest system; verified fresh builds ship latest binary.

## Summary

Implemented resilient deployment for `mmk-vst3-bridge.exe`:
- Version-stamped copy: `bridge\<timestamp>\mmk-vst3-bridge.exe`
- Manifest file: `mmk-vst3-bridge.path` (tells managed host which copy to use)
- Best-effort fallback: copy to top-level `mmk-vst3-bridge.exe` (can fail if locked; app gracefully uses versioned copy)

## Validation

✅ Fresh Debug builds contain versioned bridge matching source byte-for-byte  
✅ Manifest exists and resolves correctly  
✅ QA confirmed deployed bridge now matches rebuilt native bridge  
✅ Up-to-date tracking via MSBuild input/output tracking prevents stale deployments  
✅ Fallback gracefully warns if copy fails (expected when bridge process is running)

## Known Limitation (Acceptable)

If bridge.exe is running when building, fallback copy fails with warning. This is expected behavior (can't overwrite locked executable). Primary mechanism (versioned copy + manifest) is bulletproof and unaffected.

## Handoff

Deployment locked. Next: Coordinator validates that manifest-selected bridge works end-to-end.
