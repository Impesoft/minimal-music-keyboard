# Session Log: VST3 Audio Pipeline Fix

**Timestamp:** 2026-03-12T07:14:41Z  
**Duration:** 3 agents + 1 review  
**Commit:** 9939563 "fix: VST3 bridge now produces sound"

## Summary
Audio pipeline was completely silent. Root cause identified as missing `ProcessStartInfo.Arguments` preventing bridge process launch. Concurrent C++ fixes addressed secondary bugs (sample rate, block size, IHostApplication, QueueSetProgram). Code review flagged 2 lifetime bugs; both fixed pre-commit.

## Session Flow

### 1. Audit (agent-29 / Faye)
- Traced signal flow from UI selection → WASAPI output
- Found bridge exits immediately (`argc < 2` check fails)
- Catalogued 4 secondary bugs in C++ side
- Identified Tier 1 (minimum audio), Tier 2 (spec compliance), Tier 3–4 (ring-buffer, GUI)

### 2. Scoping (agent-30 / Jet)
- Evaluated 3 VST3 GUI hosting options
- Recommended bridge-owned Win32 popup (Option A): medium complexity, high stability
- Provided detailed IPC protocol additions
- Deferred GUI to Phase 2 (Tier 4)

### 3. C++ Fixes (agent-31 / Faye)
- Fixed sample rate mismatch: 44.1 kHz → 48 kHz
- Fixed block size mismatch: 256 → 960 samples
- Created minimal IHostApplication stub
- Fixed QueueSetProgram: output event → input MIDI bytes
- Queried and initialized IEditController

### 4. C# Fixes (agent-32 / Jet)
- Added `Arguments = $"{hostPid}"` to ProcessStartInfo (ROOT CAUSE FIX)
- Added ring-buffer read tracking via `_lastReadPos` volatile field
- Prevents stale-frame re-reads

### 5. Review (Gren)
- **APPROVED WITH CONDITIONS**
- Flagged C++ lifetime bugs: HostApplication scope, IConnectionPoint teardown
- Both fixed by Faye before commit
- C# thread safety race accepted as MVP scope

## Deliverables

| Item | Owner | Status |
|------|-------|--------|
| Audit report | Faye | ✅ `.squad/decisions/inbox/faye-vst3-audit.md` |
| C++ fixes | Faye | ✅ Source code modified |
| C# fixes | Jet | ✅ Source code modified |
| GUI scoping | Jet | ✅ `.squad/decisions/inbox/jet-vst3-gui-scope.md` |
| Code review | Gren | ✅ `.squad/decisions/inbox/gren-vst3-review.md` |

## Test Coverage Required
- Load VST3 instrument; verify audio output
- Verify pitch at 48 kHz (correct playback rate)
- Verify full 960 samples per frame (no silence padding)
- Test MIDI program change routing
- Test plugins requiring `IHostApplication` (Native Instruments, etc.)
- Verify no regression in SF2 backend

## Next Phase (Tier 4: GUI Hosting)
- Bridge-side: Win32 popup, IPlugView attachment, message pump thread
- C# side: "Open Editor" button in settings row
- IPC protocol: `openEditor`, `closeEditor`, `editorOpened`, `editorClosed`
- Estimated effort: Medium (all pieces small; integration risk main concern)

## Risk Assessment
- **Audio pipeline:** Low — root cause straightforward; C++ fixes well-scoped
- **Lifetime bugs:** ✅ Mitigated pre-commit (Faye's follow-up fixes)
- **Thread safety (C#):** Low for MVP; Phase 2 will add double-buffering
- **Regression:** None — SF2 backend unaffected; new code isolated
