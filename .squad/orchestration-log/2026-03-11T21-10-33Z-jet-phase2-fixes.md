# Orchestration Log: Jet Phase 2 Fixes

**Timestamp:** 2026-03-11T21:10:33Z  
**Agent:** Jet  
**Session ID:** (Scribe record)

---

## Summary

Jet applied all 3 Phase 2 fixes to `Vst3BridgeBackend.cs` in response to Gren's code review rejection (3 issues: 1 blocking, 2 required). Build succeeded with 0 errors.

## Fixes Applied

### Fix 1: IPC Resource Ownership (BLOCKING)
- **Issue:** Host connecting as client, bridge opening existing MMF (backwards per spec)
- **Resolution:**
  - Changed `NamedPipeClientStream` → `NamedPipeServerStream`
  - Changed `MemoryMappedFile.OpenExisting()` → `MemoryMappedFile.CreateNew()`
  - Changed PID source: `_bridgeProcess.Id` → `Process.GetCurrentProcess().Id`
  - Reordered flow: create IPC resources → launch bridge → wait → load

### Fix 2: String Allocations on Audio Thread (REQUIRED)
- **Issue:** `NoteOn()`, `NoteOff()`, `NoteOffAll()`, `SetProgram()` allocating JSON strings via interpolation on WASAPI thread
- **Resolution:**
  - Added `MidiCommand` readonly struct (discriminated union, no allocations)
  - Changed `Channel<string>` → `Channel<MidiCommand>`
  - Audio thread methods now write stack-allocated structs
  - Moved string serialization to background `RunPipeWriterAsync()` task via `SerializeCommand()`

### Fix 3: Dispose() Race (REQUIRED)
- **Issue:** Cancel tokens preempting shutdown command before drain task sends it
- **Resolution:**
  - After completing channel, wait 500ms for writer task: `_pipeWriterTask?.Wait(TimeSpan.FromMilliseconds(500))`
  - After writer exits, give bridge 2s graceful exit window: `_bridgeProcess.WaitForExit(2000)`
  - Move `_writerCts?.Cancel()` to end (cleanup only)

## Build Result

```
dotnet build --no-incremental
Build succeeded with 2 warning(s) in 8.6s
```

**Warnings:** CS0414 (unused `_frameSize` field) — harmless, retained for consistency.  
**Errors:** 0

## Files Modified

- `src/MinimalMusicKeyboard/Services/Vst3BridgeBackend.cs`

## Status

✅ **READY FOR RE-REVIEW** — Gren's approval required before Phase 3 begins.

---

**Next:** Scribe to commit all `.squad/` changes with Copilot co-author trailer.
