# Orchestration: Spike — VST3 Architecture Proposal v1.1

**Timestamp:** 2026-03-11T20:20:19 UTC  
**Agent:** Spike (Lead Architect)  
**Outcome:** COMPLETE

---

## Mandate

Revise VST3 architecture proposal to address all 6 issues (2 BLOCKING + 4 REQUIRED) from Gren's review in `docs/vst3-architecture-review.md`.

---

## Deliverables

✅ **Proposal Revision:** `docs/vst3-architecture-proposal.md` v1.1
- BLOCKING #1: Threading model contradiction (§4.2 vs §4.3) — FIXED
  - Rewrote AudioEngine code sketch; NoteOn() now enqueues to ConcurrentQueue
  - Backend drains queue on audio thread only
  - Removed _backendLock; updated threading invariants
  
- BLOCKING #2: Missing Dispose() specifications — FIXED
  - Added §4.5 "Disposal Sequence" with full code sketches
  - AudioEngine.Dispose() (6 steps), Vst3BridgeBackend.Dispose() (7 steps)
  
- REQUIRED #3: MixingSampleProvider swap semantics — FIXED
  - Clarified both backends always in mixer; inactive backends output silence
  - _activeBackend controls MIDI routing, not mixer membership
  
- REQUIRED #4: Vst3BridgeBackend state machine — FIXED
  - Added formal state machine: Running → Faulted → Disposed
  - Behavior table for Read()/NoteOn() in each state
  - Crash-to-faulted flow (7 steps); BridgeFaulted event
  
- REQUIRED #5: SoundFontBackend code sketch — FIXED
  - Full code sketch with Volatile.Read/Write, _soundFontCache, _soundFontCacheLock
  - Read() with command queue drain + synth.Render()
  - LoadAsync() with Volatile swap and proper Dispose()
  
- REQUIRED #6: IPC resource ownership — FIXED
  - Host creates MemoryMappedFile + NamedPipeServerStream
  - Bridge connects as client; host retains handles on crash
  - Naming uses host PID for stability

**Version:** 1.1  
**Status:** Pending Gren re-review

---

## Decision Artifact

Filed at `.squad/decisions/inbox/spike-proposal-v1.1-revisions.md`

---

## Notes

- All issues resolved; document now ready for re-review by Gren
- No blocking dependencies identified
- Proposal maintains backward compatibility with Phase 0 design
